1、模拟两个leader服务器，客户端分别向两台服务器各发一条数据；

2、我们封装了java.nio.channels.Selector nioSelector，定义了MySelector，里面定义了java.nio.channels.Selector nioSelector
这里使用的是典型的装饰者模式；

3、我们还自定义了一个MyKafkaChannel，这里的名字可能会引起歧义，它不是真正意义上的java nio中的channel，仅仅是一个类而已，
定义它的目的是 装载一些数据，这些数据呢是都要返给 SelectKey key里的；

4、问题：
4.1 获取消息假如消耗很长时间怎么办，是否可以设置一个最长等待时间？
4.2 参数 最大发送消息字节数、最大接受字节数都没起作用，如何才能起到作用呢？
